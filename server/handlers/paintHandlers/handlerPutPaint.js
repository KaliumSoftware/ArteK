import { Paint } from '../../controllers/paintControllers/putPaint';

const handlerPutPaint = async (
  id,
  price,
  model,
  color,
  amount,
  description,
  image
) => {
  try {
  } catch (error) {
    return error.message;
  }
};

module.exports = handlerPutPaint;
