import { Paint } from '../../models/paint';

const putPaint = async (
  id,
  price,
  model,
  color,
  amount,
  description,
  image
) => {
  try {
  } catch (error) {
    return error.message;
  }
};

module.exports = putPaint;
