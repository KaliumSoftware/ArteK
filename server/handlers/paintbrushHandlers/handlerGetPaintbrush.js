import { getPaintbrush } from '../../controllers/paintbrushControllers/getPaintbrush';

const handlerGetPaintbrush = async () => {
  try {
  } catch (error) {
    return error.message;
  }
};

module.exports = handlerGetPaintbrush;
